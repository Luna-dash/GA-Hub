# GA-Hub desktop startup investigation — 2026-08-16

- User authorized source modification and verification for the desktop shortcut startup issue.
- Real entry: Desktop GA-Hub.lnk -> `start_hidden.vbs` -> `start.bat` -> `src-tauri/target/release/ga-hub-desktop.exe`.
- Current release desktop process (latest real launch): parent `ga-hub-desktop.exe` PID 31112, sidecar PyInstaller parent PID 23508, child PID 41772.
- Rust sidecar protocol in `src-tauri/src/main.rs`: spawn with stdin/stdout pipes, read JSON `starting`, then `wait_http_ready` GET `/api/desktop/ready`; READY_TIMEOUT is 600s. It inherits sidecar stderr.
- Sidecar `server/desktop_sidecar.py`: chooses a free port by temporary socket, emits JSON `starting`, then runs Uvicorn. PyInstaller one-file creates a parent/child pair.
- Manual direct execution of the exact release sidecar with .NET redirected pipes worked: JSON starting, after ~60–100s Uvicorn became ready; `/api/desktop/ready` returned 200; newline stdin stopped it.
- Real Tauri launch did not become ready after ~140s. In latest probe, Rust desktop had an outbound SYN to sidecar port 56164; sidecar child PID 41772 had the corresponding established connection, but HTTP request failed. Thus protocol/port parsing happened, TCP accept occurred, but Uvicorn did not answer in the real Tauri context.
- Exact comparison: direct sidecar instance port 38273 returned `200 {"ok":true,"instance_token":"compare-20260816",...}` while real 56164 failed. A comparison script timed out because it synchronously drained stdout incorrectly; its process remains and must be precisely stopped (PIDs 20116/32944) before further tests.
- Repository has many pre-existing modifications. Relevant current diffs: `server/_paths.py` disables external site paths in frozen mode unless `GA_HUB_ENABLE_EXTERNAL_SITE_PATHS=1`; `server/desktop_sidecar.py` changed EOF handling so empty stdin does not stop a frozen sidecar. Release artifacts were rebuilt after these changes.
- New decisive control (2026-08-16 12:02): exact release `ga-hub-sidecar.exe`, fixed port 47937, same repo working directory, launched directly with file stdin/stdout/stderr redirection, returned HTTP 200 in 5s. Output showed Uvicorn startup and `/api/desktop/ready`; therefore the sidecar binary and fixed-port arguments are healthy. This test was PID 29592 (PyInstaller child reported PID 19880) and was stopped after verification.
- Current Rust source change: `spawn_sidecar()` now preallocates a loopback port with `TcpListener`, passes that port instead of 0, sets stdin/stdout/stderr to `Stdio::null()`, and relies only on `wait_http_ready`. Release rebuilt successfully at 11:59:50, but a clean VBS launch still had no HTTP listener after 120s (desktop PID 16944, sidecar parent 29984, child 17232). Thus the remaining difference is the Tauri-launched process context, not pipes, random-port selection, or sidecar startup code.
- Next diagnostic: launch the release desktop EXE directly (not VBS/Tauri parent chain) with the same environment and inspect whether it reaches ready; then compare parent process, environment, current directory, and inherited handles. Keep precise PID cleanup. Do not claim fixed until the actual shortcut returns HTTP 200 and the window responds.
